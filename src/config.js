// src/config.js

/**
 * URL base de la API del backend.
 * Cambia esto si tu backend corre en un puerto o dominio diferente en producción.
 */
export const API_BASE_URL = 'http://localhost:3000';

/**
 * Clave utilizada para almacenar el token JWT en localStorage.
 */
export const AUTH_TOKEN_KEY = 'lym_admin_token';
